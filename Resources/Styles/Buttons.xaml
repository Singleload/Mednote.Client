<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <!-- Button Base Style -->
    <Style x:Key="MednoteButtonBase" TargetType="{x:Type Button}" BasedOn="{StaticResource MaterialDesignRaisedButton}">
        <Setter Property="Margin" Value="4"/>
        <Setter Property="Padding" Value="16,8"/>
        <Setter Property="materialDesign:ButtonAssist.CornerRadius" Value="4"/>
        <Setter Property="materialDesign:ElevationAssist.Elevation" Value="Dp2"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="Cursor" Value="Hand"/>
    </Style>

    <!-- Primary Button -->
    <Style x:Key="MednotePrimaryButton" TargetType="{x:Type Button}" BasedOn="{StaticResource MednoteButtonBase}">
        <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="Foreground" Value="White"/>
    </Style>

    <!-- Secondary Button -->
    <Style x:Key="MednoteSecondaryButton" TargetType="{x:Type Button}" BasedOn="{StaticResource MednoteButtonBase}">
        <Setter Property="Background" Value="{StaticResource SecondaryBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource SecondaryBrush}"/>
        <Setter Property="Foreground" Value="White"/>
    </Style>

    <!-- Success Button -->
    <Style x:Key="MednoteSuccessButton" TargetType="{x:Type Button}" BasedOn="{StaticResource MednoteButtonBase}">
        <Setter Property="Background" Value="{StaticResource SuccessBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource SuccessBrush}"/>
        <Setter Property="Foreground" Value="White"/>
    </Style>

    <!-- Warning Button -->
    <Style x:Key="MednoteWarningButton" TargetType="{x:Type Button}" BasedOn="{StaticResource MednoteButtonBase}">
        <Setter Property="Background" Value="{StaticResource WarningBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WarningBrush}"/>
        <Setter Property="Foreground" Value="White"/>
    </Style>

    <!-- Error Button -->
    <Style x:Key="MednoteErrorButton" TargetType="{x:Type Button}" BasedOn="{StaticResource MednoteButtonBase}">
        <Setter Property="Background" Value="{StaticResource ErrorBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource ErrorBrush}"/>
        <Setter Property="Foreground" Value="White"/>
    </Style>

    <!-- Outlined Button -->
    <Style x:Key="MednoteOutlinedButton" TargetType="{x:Type Button}" BasedOn="{StaticResource MaterialDesignOutlinedButton}">
        <Setter Property="Margin" Value="4"/>
        <Setter Property="Padding" Value="16,8"/>
        <Setter Property="materialDesign:ButtonAssist.CornerRadius" Value="4"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="Cursor" Value="Hand"/>
    </Style>

    <!-- Record Button -->
    <Style x:Key="MednoteRecordButton" TargetType="{x:Type Button}" BasedOn="{StaticResource MaterialDesignFloatingActionButton}">
        <Setter Property="Background" Value="{StaticResource RecordingBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource RecordingBrush}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Width" Value="56"/>
        <Setter Property="Height" Value="56"/>
        <Setter Property="materialDesign:ElevationAssist.Elevation" Value="Dp6"/>
    </Style>

    <!-- Pause Button -->
    <Style x:Key="MednotePauseButton" TargetType="{x:Type Button}" BasedOn="{StaticResource MaterialDesignFloatingActionButton}">
        <Setter Property="Background" Value="{StaticResource PausedBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource PausedBrush}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Width" Value="56"/>
        <Setter Property="Height" Value="56"/>
        <Setter Property="materialDesign:ElevationAssist.Elevation" Value="Dp6"/>
    </Style>

    <!-- Stop Button -->
    <Style x:Key="MednoteStopButton" TargetType="{x:Type Button}" BasedOn="{StaticResource MaterialDesignFloatingActionButton}">
        <Setter Property="Background" Value="{StaticResource StoppedBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource StoppedBrush}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Width" Value="56"/>
        <Setter Property="Height" Value="56"/>
        <Setter Property="materialDesign:ElevationAssist.Elevation" Value="Dp6"/>
    </Style>

</ResourceDictionary>